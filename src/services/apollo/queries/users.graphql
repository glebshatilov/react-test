#import "../fragments/users.graphql"

query AllUsers {
  users {
    ...UserExtendedFields
  }
}

query SignedInUserInfo {
  user {
    ...UserExtendedFields
  }
}

query UserBySlug($userSlug: ID) {
  user(id: $userSlug) {
    ...UserExtendedFields
  }
}
