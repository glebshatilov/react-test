#import "../fragments/users.graphql"

query AllUsers {
  users {
    ...UserExtendedFields
  }
}

query SignedInUserInfo {
  user {
    ...UserBaseFields
  }
}

query UserBySlug($userSlug: ID) {
  user(id: $userSlug) {
    ...UserExtendedFields
  }
}
